# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
build/
dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local FiaOS state — should never be committed
.sessions.json
*.bak-*
/tmp/fiaos*.log

# Voice samples and model files (download separately)
voice_samples/
personaplex_mlx/

# Local config that varies per install
*.local.plist
.env
.env.local

# Personal pages / photos that live in the running static dir but are not
# part of FiaOS — never publish these
static/kids-game*.html
static/weekly-rhythm.html
static/*.png
static/*.jpg
static/*.bak-*
.revoked.json
